# 砖了个砖 - 游戏开发文档

## 一、项目概述

本项目是基于 UniApp + Vue3 + TypeScript 的跨平台三消游戏，复刻微信小程序游戏"砖了个砖"。目标平台为微信小程序、iOS、Android。

### 技术栈

| 技术 | 说明 |
|------|------|
| UniApp (Vue3) | 跨平台框架，支持 H5/小程序/App |
| TypeScript | 类型安全 |
| Pinia | 状态管理（游戏状态、计时器、消除状态等） |
| CSS (rpx) | 响应式样式单位，适配多端 |

### 项目结构

```
src/
├── types/
│   └── game.ts          # 核心类型定义（Cell, Board, MatchPair 等）
├── utils/
│   └── gameEngine.ts    # 游戏引擎核心逻辑（矩阵生成、移动、消除、卡死检测）
├── stores/
│   └── game.ts          # Pinia 状态管理（游戏状态、计时器、操作入口）
├── pages/
│   └── index/
│       └── index.vue    # 游戏主页面（UI + 交互逻辑）
├── App.vue
├── main.ts
├── manifest.json
└── pages.json
```

## 二、核心游戏规则

### 2.1 棋盘配置

- 默认配置：8×8 矩阵，6 种图标
- 图标定义：`0` 为空格，`1~6` 对应 🍎🍊🍋🍇🍓🍑
- **初始矩阵无空格**：所有格子填满图标，且保证至少存在一对可消除的相邻图标供开局
- 图标成对放置：每种图标数量为偶数，避免落单导致无法通关

### 2.2 移动规则

1. **选择起始格**：玩家按住一个非空方块作为起始格
2. **滑动方向**：支持上/下/左/右四个方向
3. **连续相邻方块整体移动**：从起始格沿滑动方向，连续的非空方块构成一组，整组平移
4. **移动范围**：组的最远端（头端）前方连续空格数即为最大可滑动格数；碰到边界或非空方块即停
5. **方向不锁定**：拖动过程中方向可以切换（如先右后左），只要在可移动范围内即可
6. **方向实时计算**：拖动方向由实时位移决定，方向变化时重新计算可移动组与最大步数，杜绝穿越非空方块
7. **实时预览**：拖动过程中方块实时跟随手指/鼠标移动，松手后吸附到最近格子

### 2.3 消除规则

1. **隔空相邻消除**：同一行/列中，两个相同图标方块之间只存在空格（无其他图标阻挡），则可消除
2. **仅判别被操作方块**：
   - 移动触发：仅判别被点击（移动）的那个方块，不判别也不消除其他对子
   - 点击触发：仅判别被点击的那个方块
3. **四方向判别**：无论移动还是点击，均判别上下左右四个方向（统一逻辑）
4. **单次操作最多消除一次**：一次松手/点击最多消除一对，不触发连锁消除
5. **多对选择**：若被操作方块的上下左右存在多个相同图标，不能三个一起消除，而是高亮所有候选方块（橙色描边 + 脉冲动画），玩家必须选择消除其中一对（无取消选项）

### 2.4 点击消除

- 若方块已有隔空相邻的同图标对，直接点击即可消除，无需移动
- 点击消除的判别逻辑与移动触发完全一致（四方向判别）

### 2.5 无效操作反馈

- **移动后无消除对**：方块回弹到原位，棋盘抖动反馈
- **点击后无消除对**：矩阵中所有与点击方块相同图标的方块原地抖动（400ms）

### 2.6 消除后方块位置

- 消除后，一起移动的未消除方块**留在移动后位置**，不回弹到原位

### 2.7 游戏目标与结束条件

| 条件 | 说明 |
|------|------|
| 通关 | 消除全部图标，矩阵清空 |
| 游戏失败 | 无棋可走（无可点击消除对，且无任何移动能创造消除对） |
| 计时器 | 从游戏开始（页面加载）即运行，通关或失败时停止 |

## 三、核心逻辑详解

### 3.1 类型定义 (`src/types/game.ts`)

```typescript
type IconId = number          // 0=空格, 1..N=图标
type Board = IconId[][]       // 二维矩阵 board[row][col]
interface Cell { row, col }   // 格子坐标
type Direction = 'up' | 'down' | 'left' | 'right'
interface GameConfig { rows, cols, iconCount }
interface MatchPair {
  anchor: Cell    // 被操作的方块（起点）
  target: Cell    // 配对的方块（需高亮让玩家选择的）
  icon: IconId
}
```

### 3.2 游戏引擎 (`src/utils/gameEngine.ts`)

| 函数 | 说明 |
|------|------|
| `generateBoard(config, emptyRatio=0)` | 生成初始矩阵，emptyRatio=0 时无空格，图标成对放置 |
| `getRun(board, start, dir)` | 计算从起始格沿方向的连续相邻方块组 |
| `getMaxSlide(board, start, dir)` | 计算整组最大可滑动格数 |
| `applyMove(board, start, dir, steps)` | 执行移动，返回新矩阵（非法返回 null） |
| `findPairsForCell(board, cell)` | 查找与指定方块构成隔空相邻对的所有方块（四方向，最多4对） |
| `findCandidateGroups(board)` | 查找全盘所有可消除对（逐行逐列扫描） |
| `eliminate(board, cells)` | 消除指定方块（置空），返回新矩阵 |
| `isCleared(board)` | 判断是否通关（全部消除） |
| `isStuck(board)` | 判断是否无棋可走（游戏失败） |
| `canAnyMoveCreatePair(board)` | 遍历所有可能移动，判断是否存在能创造消除对的移动 |

### 3.3 状态管理 (`src/stores/game.ts`)

#### 响应式状态

| 状态 | 类型 | 说明 |
|------|------|------|
| `board` | `Board` | 当前棋盘矩阵 |
| `selected` | `Cell \| null` | 当前选中的格子 |
| `elapsedMs` | `number` | 已用时（毫秒） |
| `won` | `boolean` | 是否通关 |
| `stuck` | `boolean` | 是否无棋可走（游戏失败） |
| `invalidShake` | `boolean` | 非法移动反馈（棋盘抖动） |
| `shakeIconId` | `IconId \| null` | 点击未消除时抖动的图标 ID |
| `candidates` | `MatchPair[]` | 候选消除对列表 |
| `pendingBoard` | `Board \| null` | 候选选择态的棋盘快照 |

#### 核心方法

| 方法 | 说明 |
|------|------|
| `tryMove(start, dir, steps)` | 执行移动+消除判别，单对直接消除，多对进入候选选择态 |
| `tryClickEliminate(cell)` | 点击直接消除判别，逻辑与移动一致 |
| `chooseAndEliminate(pair)` | 玩家选择消除某候选对 |
| `checkWin()` | 检测通关或失败（同时判断 isCleared 和 isStuck） |
| `reset()` | 重置游戏（重新生成棋盘，确保非卡死，启动计时器） |
| `isChoosing()` | 是否处于候选选择态 |
| `startTimer()` / `stopTimer()` | 计时器控制 |
| `formatTime()` | 格式化用时为 MM:SS |

#### 操作流程

```
玩家操作
├── 拖动移动
│   ├── onDown: 记录起始格
│   ├── onMove: 实时计算方向+位移+预览
│   └── onUp:
│       ├── 无方向/无可移动空间 → tryClickEliminate (点击消除)
│       ├── 预判有消除对 → 吸附动画 → tryMove
│       └── 预判无消除对 → 回弹动画
│
└── tryMove 逻辑:
    ├── applyMove 计算移动后矩阵
    ├── findPairsForCell 判别被操作方块的消除对
    ├── 无消除对 → 抖动反馈，不提交移动
    ├── 仅一对 → 提交移动 + 消除 + checkWin
    └── 多对 → 提交移动 + 进入候选选择态
        └── 玩家选择 → chooseAndEliminate → 消除 + checkWin
```

### 3.4 交互层 (`src/pages/index/index.vue`)

#### 拖拽实现

- 同时绑定 touch 事件和 mouse 事件（兼容移动端和桌面端浏览器）
- 触摸/鼠标事件防冲突：触摸后 500ms 内忽略鼠标事件
- 方向确定阈值：位移 ≥ 6px 才确定方向
- 吸附动画：松手后 180ms 动画过渡到目标格
- 坐标换算：rpx ↔ px 基于 `uni.getSystemInfoSync().windowWidth`

#### 候选选择态

- 多对消除时，候选方块（target）高亮：橙色描边 + 脉冲动画
- 玩家点击高亮方块即消除该候选对
- 候选选择态期间禁止其他交互（移动和点击）
- 必须选择一组消除，无取消选项

#### 弹层

| 弹层 | 触发条件 | 内容 |
|------|----------|------|
| 通关弹层 | `store.won === true` | 🎉 通关 + 用时 + 再来一局按钮 |
| 失败弹层 | `store.stuck === true` | 😢 游戏失败 + 用时 + 重新开始按钮 |

弹层覆盖全屏，阻止穿透，点击按钮调用 `store.reset()` 重开。

## 四、开发规范与约束

### 4.1 硬性约束

1. **图标成对生成**：所有图标必须成对放置，防止落单导致游戏无法结束
2. **消除后位置保留**：消除后未消除的方块留在移动后位置，不回弹
3. **单次消除**：单次操作最多消除一对，不触发连续消除
4. **多对必选**：存在多对消除候选时，玩家必须选择消除一对，无取消选项
5. **计时器即时启动**：计时器从游戏开始就运行，而非首次操作后
6. **初始无空格**：初始矩阵填满所有格子
7. **初始可玩性保证**：`reset()` 中检测初始棋盘是否卡死，若卡死则重新生成（最多重试 10 次）

### 4.2 工程约定

1. **拖动方向实时计算**：方向由实时位移决定，可在同一拖拽过程中切换方向
2. **方向切换重新计算**：方向变化时重新计算可移动组和最大步数，防止穿越
3. **点击消除与移动消除统一**：均使用 `findPairsForCell` 四方向判别
4. **无移动空间时降级为点击**：`onUp` 中 `maxSteps=0` 时自动调用 `tryClickEliminate`
5. **游戏结束状态守卫**：`won` 或 `stuck` 为 true 时禁止所有交互
6. **矩阵不可变**：所有矩阵操作返回新矩阵（`cloneBoard`），不直接修改原矩阵

### 4.3 样式规范

- 使用 `rpx` 单位适配多端
- 单元格尺寸：76rpx，间距 6rpx
- 颜色方案：
  - 选中态：橙色描边 `#ff9800`
  - 候选态：红橙色描边 `#ff5722` + 脉冲动画
  - 通关：绿色 `#4caf50`
  - 失败：红色 `#f44336`
- 动画时长：吸附/回弹 180ms，抖动 300-400ms

## 五、已知问题与待办

### 5.1 待移除的测试代码

`src/utils/gameEngine.ts` 的 `generateBoard` 函数中存在一段临时的测试代码（`stuckPattern`），硬编码返回一个必死棋盘用于测试游戏失败功能。该段代码标记为 `TEMP TEST`，上线前必须移除，恢复正常的随机生成逻辑。

```typescript
// 需要移除的代码片段（约第 292-304 行）：
// TEMP TEST: 返回一个必死棋盘
const stuckPattern = [...]
return stuckPattern as Board
// TEMP TEST END
```

### 5.2 已解决的问题（历史经验）

| 问题 | 根因 | 解决方案 |
|------|------|----------|
| 无法拖动方块 | 只绑定了 touch 事件 | 添加 mouse 事件支持，统一坐标获取 |
| 移动穿越非空方块 | 拖动方向锁定未重新计算 | 方向变化时实时重算可移动组与最大步数 |
| 消除后方块回弹 | `commitMoveAndEliminate` 基于原矩阵恢复 | 改用 `eliminate` 直接保留移动后位置 |
| 移动方向轴限制消除 | 水平移动只判纵向、垂直只判横向 | 统一为四方向判别（`findPairsForCell`） |
| 图标落单无法通关 | 生成时未保证成对 | 图标成对放置，数量为偶数 |
| 初始棋盘可能卡死 | 随机生成可能产生无解棋盘 | `reset()` 中检测卡死并重新生成 |

## 六、构建与运行

```bash
# 类型检查
npm run type-check

# H5 开发模式
npm run dev:h5

# 微信小程序开发模式
npm run dev:mp-weixin

# 构建微信小程序
npm run build:mp-weixin
```

## 七、默认配置

| 配置项 | 值 | 说明 |
|--------|-----|------|
| rows | 8 | 棋盘行数 |
| cols | 8 | 棋盘列数 |
| iconCount | 6 | 图标种类数 |
| emptyRatio | 0 | 初始空格比例（0 = 无空格） |

可通过修改 `DEFAULT_CONFIG` 调整棋盘大小和图标种类数。
